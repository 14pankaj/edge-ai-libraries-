services:
  model_download_service:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "32004:8000"  # Map container port 8000 to host port 8000
    volumes:
      # Mount a volume for caching Hugging Face models
      - ~/.cache/huggingface:/root/.cache/huggingface
      - ~/models:/app/models